# API (apps/api)
DATABASE_URL=
JWT_SECRET=
ML_ENGINE_URL=http://localhost:8000
PORT=3001

# Web (apps/web) — Vite exposes only VITE_* vars to the client
VITE_API_URL=http://localhost:3001

# Mobile (apps/mobile)
EXPO_PUBLIC_API_URL=http://localhost:3001/api

# ML Engine (apps/ml-engine)
# DATABASE_URL is shared with API above
